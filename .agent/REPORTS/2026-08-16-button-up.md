# Button-Up Report — camping4you.net

GENERATED-BY: recipes/button-up-project.sh
**Date:** 2026-08-16
**Target:** `/Users/stephen/Projects/camping4you.net`
**dotagent install:** `/Users/stephen/Projects/dotagent`

Propagates dotagent conventions into the target project. CLAUDE.md
refresh is the only mutation; drift findings are advisory.

---

## 1. CLAUDE.md refresh

✓ Refreshed `/Users/stephen/Projects/camping4you.net/CLAUDE.md` via publish/publish.sh with the
project's pairings bundled in. Pulls latest operating manual +
principles + interaction style from personal/ + the manual.

---

## 2. drift-check findings

GENERATED-BY: drift-check/drift-check.sh
**Target:** `/Users/stephen/Projects/camping4you.net/.agent`
**Date:** 2026-08-16
**Checks run:** 32

## ✓ assumption-shape-change

Clean.

## ✓ authority-surface

Clean.

## ✓ carry-forward-cap

Clean.

## ✓ checked-todo-accumulating

Clean.

## ✓ checkins-sync

Clean.

## ✓ cross-repo-plan-migration

Clean.

## ✓ decision-unconverged

Clean.

## ✓ depends-resolvable

Clean.

## ✓ dotagent-db-tracked

Clean.

## ✗ frontier-dead (1 finding(s))

- ROADMAP.md: the ready frontier is empty (no open `- [ ]` task under `## Active` or `## Loose`) yet 1 behavior commit(s) landed in the last 7d, oldest 98369ce (2026-08-10). Work is shipping with no task to track it: open a milestone, or record the work as `## Loose` one-liners.

## ✓ githook-stale

Clean.

## ✓ live-field-usage

Clean.

## ✓ milestone-unbounded

Clean.

## ✓ pairing-polish-cadence

Clean.

## ✓ preflight-unanswered

Clean.

## ✓ proposed-decisions-stale

Clean.

## ✓ proposed-without-ratify-line

Clean.

## ✓ queued-items-expiry

Clean.

## ✗ report-without-disposition (3 finding(s))

- REPORTS/research-cloudflare-content-store.md is 49d old with no disposition block; route its findings per D-0036 (fixed | queued | idea'd | dismissed)
- REPORTS/research-oidc-multi-idp.md is 49d old with no disposition block; route its findings per D-0036 (fixed | queued | idea'd | dismissed)
- REPORTS/research-telnyx-a2p-10dlc.md is 49d old with no disposition block; route its findings per D-0036 (fixed | queued | idea'd | dismissed)

## ✗ reports-untriaged (4 finding(s))

- REPORTS/project-brief.md is 49d old and has no `## Triage` section; archive per D-0017
- REPORTS/research-cloudflare-content-store.md is 49d old and has no `## Triage` section; archive per D-0017
- REPORTS/research-oidc-multi-idp.md is 49d old and has no `## Triage` section; archive per D-0017
- REPORTS/research-telnyx-a2p-10dlc.md is 49d old and has no `## Triage` section; archive per D-0017

## ✗ roadmap-done-unmigrated (2 finding(s))

- ROADMAP.md line 30 (## Backlog): done task `B6` is 49d old and still in the live frontier; migrate it to ROADMAP-SHIPPED.md (`roadmap-render --harvest` proposes the block)
- ROADMAP.md line 37 (## Backlog): done task `B8` is 49d old and still in the live frontier; migrate it to ROADMAP-SHIPPED.md (`roadmap-render --harvest` proposes the block)

## ✓ roadmap-grain

Clean.

## ✓ roadmap-id-collision

Clean.

## ✓ roadmap-task-grammar

Clean.

## ✗ scope-inventory-drift (2 finding(s))

- capability 'robots.txt' (src/routes/robots.txt/) implemented in code but absent from PROJECT-SCOPE.md inventory
- capability 'sms-sample' (src/routes/sms-sample/) implemented in code but absent from PROJECT-SCOPE.md inventory

## ✓ scoping-without-prior-art

Clean.

## ✓ session-read-oversize

Clean.

## ✓ stale-ephemeral-artifact

Clean.

## ✓ state-prior-stack

Clean.

## ✓ tree-anchors-resolve

Clean.

## ✓ tree-coverage

Clean.

## ✗ verified-stamp-stale (1 finding(s))

- ROADMAP.md line 33 (## Backlog): open task `B11` is absence-shaped (matched `none`) and carries no `verified:` stamp, so the board cannot say when this line's premise was last checked at source. Re-measure at source before briefing this line, then record the day you looked as a `— verified: YYYY-MM-DD` field (D-0108 part 1). This check reads claim SHAPE only and makes no finding about what the premise asserts

---

**Summary:** 13 finding(s) across 32 check(s); 26 clean.

Advisory only — drift-check never modifies files. Triage findings by
hand or via `recipes/button-up-project.sh` for an adopting project.

---

## 3. decision-log audit

GENERATED-BY: decision-log/decision-log.sh audit
**Target:** `/Users/stephen/Projects/camping4you.net/.agent/DECISIONS`
**Date:** 2026-08-16
**Files:** 1 decision file(s); 1 index row(s)

## ✓ Clean

All 1 Binding decision file(s) match index row(s) in title and filename.

---

## 4. Pairings polish advisory

Pairing-polish cadence is a drift-check check (`pairing-polish-cadence`),
run as part of §2 above: any pairing selected in this project's
PROJECT-SCOPE.md that has not been polished within the quarterly (>90d)
cadence is flagged there. Detection lives in the engine; this recipe
only sequences it (per D-0019; recipe stays free of engine-grade work).

To polish a pairing flagged stale in §2:
```
/Users/stephen/Projects/dotagent/polish-pairing/polish-pairing.sh --name <name> --out /tmp/<name>-pack.md
```
Then conduct the polish in a Claude session and save the revised draft
back to `/Users/stephen/Projects/dotagent/pairings/<name>.md`.

---

## Next steps

1. Triage drift findings above (§2 + §3). Each finding is a
   candidate for an edit to the target project's `.agent/`
   surface — append `Triage` notes to stale reports, sync
   `PROJECT-STATE.md` §3 with `CHECKINS/`, update `§5b`
   queued items with by-date/trigger, etc. Per D-0017, D-0019.
2. If pairings were flagged (§4), polish them via
   `polish-pairing/` and update the pairing files in dotagent.
3. CLAUDE.md is now refreshed (unless `--no-publish` was given).
   The target project's next Claude session will see the latest
   operating manual + pairings.
4. Once triage is complete, append a `## Triage` section to this
   report and `mv` to `/Users/stephen/Projects/camping4you.net/.agent/REPORTS/ARCHIVED/` per D-0017.

Per Principle 4, button-up-project.sh does not auto-fix drift —
findings are advisory; the human (or a Claude session inside the
target project) applies edits.

---

## Triage — 2026-08-16

Dispositions per D-0036. Re-ran `drift-check` after the fixes: **13 → 10
findings**.

| Finding | Disposition |
|---|---|
| `roadmap-done-unmigrated` (B6, B8) | **fixed** — plus B7, which the check had not yet aged into. Applied `roadmap-render --harvest`: created `.agent/ROADMAP-SHIPPED.md`, moved all three done tasks (with B6's residual continuation line) verbatim, deleted them from the live frontier, re-rendered TODO. Check now clean. |
| `verified-stamp-stale` (B11) | **fixed** — B11's premise (both photo slots empty, pipeline green while empty) was measured at source this session by building the site with and without synthetic originals. Stamped `— verified: 2026-08-16`. Check now clean. |
| `frontier-dead` | **dismissed-with-reason** — the shipped commit (98369ce) *was* tracked, as B7; it just lived in `## Backlog` rather than an active milestone. No milestone is active by design: M1 and M3 shipped, M2 is gated on Telnyx A2P registration, and every open task is `held:` on owner creds or a decision. An empty ready frontier is the honest state here, not untracked work. Revisit when M2 opens. |
| `reports-untriaged` (4) · `report-without-disposition` (3) | **queued** — pre-existing (49d), all four are the M1-era research/brief reports. Already tracked: **B2** folds the three research reports into ratified DECISIONs, which is the act that produces their dispositions. Archiving `project-brief.md` per D-0017 rides along with it. |
| `scope-inventory-drift` (2) | **queued** — `robots.txt` and `sms-sample` are implemented but absent from the PROJECT-SCOPE inventory. Pre-existing and correct as a finding, but `PROJECT-SCOPE.md` is the owner's surface and the criticality rubric makes scope edits Critical; not amended unilaterally at session wrap. |

### One open question for the owner

`ROADMAP.md` still carries its own `## Shipped` section (the M1 and M3
milestone blocks) now that `ROADMAP-SHIPPED.md` exists — two homes for shipped
history. `--harvest` proposed only the task migration, so the milestone blocks
were left alone rather than restructured unasked. Worth collapsing into one
home next session.

## Disposition — 2026-09-17
- frontier-dead / roadmap-done-unmigrated / verified-stamp-stale — fixed (resolved in commit 784240d; today's drift run shows all three clean).
- report-without-disposition (3) — fixed-now: Triage + disposition blocks added to the three research reports, all archived (see ROADMAP B2 for the routing).
- reports-untriaged (4) — fixed-now: same three archived; project-brief.md dismissed-with-reason (it is the locked vision brief, an authority-surface reference, not a findings report — retained at root because PROJECT-STATE §1 cites it there).
- scope-inventory-drift (2) — fixed-now: robots.txt + sms-sample added to PROJECT-SCOPE capability inventory addendum (2026-09-17).
